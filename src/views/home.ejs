<!DOCTYPE html>
<html lang="es">
	<head>
		<meta charset="UTF-8" />
		<meta
			name="viewport"
			content="width=device-width, initial-scale=1.0" />
		<title>Inicio</title>
		<link
			rel="stylesheet"
			href="/styles/style.css" />
	</head>
	<body>
		<nav>
			<!-- Agrega enlaces de navegación aquí -->
		</nav>

		<header>
			<h1 class="text-3xl">Bienvenido al Foro</h1>
			<p>¡Únete a la conversación!</p>
			<p>
				<a
					href="/add"
					class="inline-block bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline"
					>Crear publicación</a
				>
			</p>
		</header>

		<main>
			<!-- Muestra las publicaciones del foro aquí -->
			<% if (posts && posts.length > 0) { %>
			<ul>
				<% posts.forEach(function(post) { %>
				<li>
					<h2><a href="/post/<%= post.id %>"><%= post.title %></a></h2>
					<p><%= post.content %></p>
					<p>Por: <%= post.author %></p>
				</li>
				<% }); %>
			</ul>
			<% } else { %>
			<p class="text-red-500">No hay publicaciones disponibles.</p>
			<% } %>
		</main>

		<footer>
			<p>&copy; 2023 Foro de Discusión</p>
		</footer>
	</body>
</html>
